% Preamble
\documentclass[tikz,border=12pt,12pt]{standalone}
\input{style/new_sequence.tikz}
\input{vars/shortcuts.tex}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric}


\begin{document}
\begin{tikzpicture}

% Body
   % tx types
   \node (authorization) [tx] at (5,1.6) {authorization};
   \node (decision1) [decision] at (5,-2.2) {Reserved\\ funds\\ captured?};
   \node (void-authorization) [tx] at (11.1,-2.2) {void-authorization};
   \node (capture-authorization) [tx] at (5,-6.5) {capture-authorization};
   \node (void-capture) [tx] at (10.56,-10.5) {void-capture};
   \node (refund-capture) [tx] at (-0.6,-10.5) {refund-capture};
   \node (decision2) [decision] at (5,-10.5) {Funds\\ transfer\\ triggered?};

% Arrows & Text
  \draw[solidarrow, txt, text width=9cm]
                      	(authorization.south) -- (decision1.north);
 \draw[solidarrow]
                      	(decision1.east) -- (void-authorization.west) node [yes_no] {yes}; 
  \draw[solidarrow]
                      	(decision1.south) -- (capture-authorization.north) node [yes_no] {no}; 
  \draw[solidarrow]
                      	(capture-authorization.south) -- (decision2.north);     
  \draw[solidarrow]
                      	(decision2.west) -- (refund-capture.east) node [yes_no] {no}; 
  \draw[solidarrow]
                      	(decision2.east) -- (void-capture.west) node [yes_no] {yes};                       
                      
\end{tikzpicture}
\end{document}
